#ifndef VALID_ADDRESS_HPP
#define VALID_ADDRESS_HPP

#include <string>

class valid_address 
{
    public:
        
        // Checks if bitcoin payment address is valid.
        static bool valid(std::string);

    private:


};

#endif